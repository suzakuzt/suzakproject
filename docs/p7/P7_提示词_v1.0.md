# P7 H5 前端开发提示词

你是本项目的 **H5 前端开发 Agent**。

## 当前任务

只实现 **P7 活动规则页 / 规则说明页**。

## 重要限制

1. 只处理 P7，不要开发 P1、P2、P4、P5、P6 或其他页面的完整业务。
2. P7 只做活动规则展示和企微二维码展示，不在本页处理抽签、领奖、用券或商品跳转。
3. 不要新增未确认的业务规则。
4. 不要改动与 P7 无关的页面逻辑。
5. 不要私自改文案。
6. 第 4 条规则按最新业务口径：完成抽签后自动点亮 1 天进度，每天最多点亮 1 天；不要写成必须领取奖励后才点亮。

## 输入资料

- `P7_H5前端_Agent任务卡.md`
- P7 主设计稿
- 当前项目目录结构
- 当前前端技术栈说明
- 当前路由配置
- 当前埋点工具说明

## P7 已确认规则

1. 本页为活动规则独立页面。
2. 页面标题固定展示「活动规则」。
3. 副标题固定展示「一举高中 · 六月牛气加油签」。
4. 左上角返回按钮返回上一页；无历史栈时回 P1 活动首页。
5. 页面展示 1-7 条规则。
6. 每人每日默认 1 次抽签机会。
7. 分享好友参与并完成抽签后，原用户增加 1 次助力进度。
8. 每日通过分享最多获得 3 次额外机会。
9. 每日完成抽签后，自动点亮 1 天进度，每天最多点亮 1 天。
10. 累计点亮 7 天或邀请 5 位好友，可解锁 985 和牛礼盒抽奖资格。
11. 奖品数量有限，具体以活动页面公示为准。
12. 本活动为娱乐互动与祝福活动，不代表任何成绩预测。
13. 底部展示企微二维码，文案为「扫码添加企微」「进群可获取活动提醒、专属优惠及福利通知」。
14. 当前页不默认出现抽签按钮、奖励卡、商品推荐、再抽一次、签文结果或 AI 解签正文。

## 需要实现

1. 按设计稿还原 P7 活动规则页。
2. 实现深红背景、顶部标题、副标题、牛肉礼盒氛围图、规则卡、企微二维码模块。
3. 实现规则列表展示，规则文案支持后端配置或本地配置。
4. 接口失败时必须使用本地兜底规则，不能出现空白页。
5. 实现左上角返回按钮，优先返回上一页，无历史栈时回 P1 首页。
6. 实现企微二维码展示，二维码资源支持配置或静态替换。
7. 支持页面纵向滚动和不同手机安全区适配。
8. 接入埋点：页面曝光、渲染成功 / 失败、返回点击、二维码曝光、二维码加载失败、滚动到底部。
9. 完成后请运行项目现有的 `lint / build / test` 命令，并说明结果。

## 交付结果

1. 修改了哪些文件。
2. 新增了哪些文件。
3. P7 页面如何访问；正式路由为 `/activity/rules`，当前代码内部暂以 `rules` 表示 P7。
4. P6「活动规则」入口如何跳转到 P7。
5. 返回按钮如何处理无历史栈场景。
6. 规则文案和二维码是否还有 mock / 本地配置。
7. 如何自测。
8. `lint / build / test` 运行结果。
