你是本项目的 H5 前端开发 Agent。
当前任务：
只实现 P1 活动首页 / 活动入口页。
重要限制：
1. 只处理 P1，不要开发 P2、P6、活动规则页的完整业务。
2. P2、P6、活动规则页已有对应页面文档；当前代码仍以本地状态切页为主，真实路由 / 端跳转后续统一接入。
3. 不要新增未确认的业务规则。
4. 不要改动与 P1 无关的页面。
5. 不要私自改文案。
输入资料：
- P1_H5前端_Agent任务卡.md
- P1 设计图
- 当前项目目录结构
- 当前前端技术栈说明
P1 已确认规则：
1. 主按钮固定为「立即摇签」。
2. 新用户默认展示「我的摇签机会 1次」。
3. 签筒 5 支签不参与真实签文，只作为首页视觉和后续动态效果素材。
4. 点击签筒等同于点击「立即摇签」。
5. 活动规则跳转 P7 活动规则页。
6. 我的奖励跳转 P6 我的奖励页。
7. 分享获取次数打开分享引导 / 分享海报占位弹窗。
8. 分享行为不限制；每日最多奖励 3 次摇签机会，超过 3 次继续允许分享但不再增加机会。
9. 首页不展示「今日已摇签」按钮。
10. 有额外摇签机会时，主按钮仍为「立即摇签」，只更新底部机会数量。
11. P1 不处理活动结束态，活动结束后入口直接下架。
需要实现：
1. 按设计稿还原 P1 首页。
2. 实现底部「我的摇签机会 N次」动态展示，默认 1 次。
3. 实现点击「立即摇签」和点击签筒的同一入口逻辑。
4. 如果机会 > 0，进入 P2 今日考运签结果页。
5. 如果机会 = 0，提示「今日机会已用完，分享可再得机会」。
6. 活动规则跳转 P7 活动规则页。
7. 我的奖励跳转 P6 我的奖励页。
8. 分享获取次数打开分享引导弹窗。
9. 保留后续接接口的位置，不要把业务写死。
10. 完成后请运行项目现有的 lint / build / test 命令，并说明结果。
交付结果：
1. 修改了哪些文件。
2. 新增了哪些文件。
3. P1 页面如何访问。
4. 如何自测。
5. 是否还有未接入的 mock 数据或占位逻辑。
